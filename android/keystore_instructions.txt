# Perintah untuk membuat Keystore secara manual di terminal:
# keytool -genkey -v -keystore release.keystore -alias billiards_alias -keyalg RSA -keysize 2048 -validity 10000 -storepass password123 -keypass password123 -dname "CN=Billiards, OU=Gaming, O=BilliardsApp, L=Jakarta, S=Jakarta, C=ID"

# Catatan: Password default yang digunakan di GitHub Action adalah 'password123'
# Anda bisa menggantinya di file .github/workflows/android.yml jika diperlukan.

# --- INSTRUKSI IKON ---
# 1. Simpan gambar ikon yang Anda berikan sebagai 'ic_launcher.png'.
# 2. Letakkan file tersebut di folder:
#    android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
# 3. Android Studio akan secara otomatis menyesuaikan ukurannya untuk resolusi lain.

